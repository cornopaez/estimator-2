var fs = require('fs');
var path = require('path');

var creds = require('./data/creds');
var PokitDok = require('./index');
var pokitdok = new PokitDok(creds.POKITDOK_CLIENT_ID, creds.POKITDOK_CLIENT_SECRET);

var claimStream = fs.createReadStream('./data/claim.270');
pokitdok.files({
    'trading_partner_id': 'MOCKPAYER'
}, claimStream, function (err, res) {
    console.log(err || res || 'dang');
});